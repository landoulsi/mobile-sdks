# `:document`

> **Status: scaffolding.** This module is a KMP library skeleton with no feature
> surface yet.

## What's here

- `commonMain/.../Platform.kt` — `expect fun platform(): String`
- `androidMain` / `iosMain` — the `actual` implementations
- `ExampleUnitTest` / `ExampleInstrumentedTest` — generated placeholder tests

## Platforms

Android + iOS (`com.android.kotlin.multiplatform.library`, namespace
`com.landoulsi.document`).

Document-handling APIs will be added here; until then there is nothing to depend
on.
