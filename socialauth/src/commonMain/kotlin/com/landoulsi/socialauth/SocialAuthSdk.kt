package com.landoulsi.socialauth

/**
 * Entry point metadata for the `:socialauth` module.
 *
 * The functional surface lives on [SocialAuthClient] (obtained from
 * [SocialAuthClientFactory]); this object only carries build information.
 */
object SocialAuthSdk {
    const val VERSION = "1.0.0"
}
